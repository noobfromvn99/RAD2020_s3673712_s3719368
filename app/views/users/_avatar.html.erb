<br>
<%= form_for(current_user, url: avatar_upload_path, method: :patch ) do |f| %> 
    <%= f.file_field :avatar, id:"file-input", accept:'image/jpeg,image/gif,image/png'%>
    <%= f.submit "upload", class:"btn btn-primary"%>
<%end%>


no larger than 2mb


<scripttype="text/javascript">
  $('#file-input').bind('change',function() {varsize_in_megabytes=this.files[0].size/1024/1024;
  if(size_in_megabytes > 2 ) {
    alert('Maximum file size is 2MB. Please choose a smaller file.');
    }
  });
</script>
